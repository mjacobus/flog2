<article>
  <div class="hero-unit">
    <h1><%= @album.title %></h1>
  </div>
  
  
  <div class="row-fluid">
    <div class="span12">
      <%= simple_format(@album.body) if @album.body? %>
    </div>
  </div>

  <% @album.pictures.each do |picture| %>
    <div class="row-fluid">
      <div class="span12">
        <%= picture(picture) %>
      </div>
    </div>
  <% end %>

</article>