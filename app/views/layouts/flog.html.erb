<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Pamela Jacobus Fotografia" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= google_analytics %>
    <%= facebook_like_meta %>
  </head>
  <body id="flog">
    <%= facebook_like_header %>
    <div class="container-fluid">
      <div class="row-fluid">
        <aside class="span3">
          <div class="">
            <a href="/">
              <%= image_tag('logo-original.png', alt: 'Pamela Jacobus Fotografia', title: 'Pamela Jacobus Fotografia', id: 'logo') %>
            </a>

            <ul class="nav nav-list">
              <li class="nav-header"><%= t('flog.categories') %></li>
              <% categories.each do |category| %>
                <li><%= link_to category.name, category_path(category) %></li>
              <% end %>
              <% menu_pages.each do |page| %>
                <li><%= link_to(page.title, "/#{page.slug}.html") %></li>
              <% end %>
              <li class="nav-header"><%= t('flog.about') %></li>
              <li><a target="_blank" href="http://www.facebook.com/PamelaJacobusFotografia" class="sprite facebook_page">Minha Página no Facebook</a></li>
              <li><a href="/contato">Contato</a></li> 
            </ul>
          </div><!--/.well -->
        </aside><!--/aside-->
        <div class="span9">
          <%= bootstrap_flash %>
          <%= yield %>
        </div>
      </div><!--/row-->
    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
