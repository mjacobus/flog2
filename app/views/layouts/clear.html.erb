<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Pamela Jacobus Fotografia" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= google_analytics %>
    <%= facebook_like_meta %>
  </head>
  <body id="clear">
    <%= facebook_like_header %>
    <div class="container-fluid">
          <%= bootstrap_flash %>
          <%= yield %>

          <div class="row-fluid">
            <div class="span3"></div>
            <div class="span3">
              <ul class="nav nav-list">
                <li class="nav-header">Categorias</li>
                <% categories.each_with_index do |category, index| %>
                  <li><%= link_to category.name, category_path(category) %></li>
                <% end %>
              </ul>
            </div>
            <div class="span3">
              <ul class="nav nav-list">
                <li class="nav-header">Quem sou eu</li>
                <li><a target="_blank" href="http://www.facebook.com/PamelaJacobusFotografia" class="sprite facebook_page">Facebook</a></li>
                <li><a href="/contato">Contato</a></li> 
              </ul>
            </div>
            <div class="span3"></div>
          </div>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= javascript_include_tag "application" %>

  </body>
</html>
